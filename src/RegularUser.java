public class RegularUser extends User{
    public RegularUser(String userName, String emailAddress, String password, String userID, String userType) {
        super(userName, emailAddress, password, userID, userType);
    }
}
